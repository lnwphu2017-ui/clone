# 📘 รายละเอียดทางเทคนิค: ระบบ RAG & Embedding (DayOne Project)

ระบบนี้ถูกออกแบบมาเพื่อช่วยให้ AI สามารถตอบคำถามโดยอ้างอิงจาก "ฐานความรู้ส่วนตัว" (Knowledge Base) ได้อย่างแม่นยำ โดยไม่ต้องส่งข้อมูลทั้งหมดให้ AI ตลอดเวลา ซึ่งจะช่วยทั้งในเรื่องความประหยัดและประสิทธิภาพครับ

---

## 1. RAG คืออะไร? (Retrieval-Augmented Generation)
**RAG** คือกระบวนการที่ AI ทำการ "ค้นหา" ข้อมูลที่เกี่ยวข้องจากฐานข้อมูลก่อน แล้วค่อย "สร้างคำตอบ" โดยอ้างอิงจากข้อมูลที่หามาได้ เปรียบเสมือน AI มีหนังสือไว้เปิดอ้างอิงก่อนตอบคำถามครับ

---

## 2. ขั้นตอนการทำงาน (Workflow)

กระบวนการแบ่งออกเป็น 2 เฟสหลักๆ ดังนี้ครับ:

### เฟสที่ 1: การเตรียมข้อมูล (Indexing Phase)
ทำเพียงครั้งเดียวเมื่อมีการเพิ่มหรืออัปเดตไฟล์ในโฟลเดอร์ `api/knowledge`
1.  **Parsing:** ระบบอ่านไฟล์ (.txt, .pdf, .docx) และสกัดข้อความดิบออกมา
2.  **Chunking:** ตัดข้อความที่ยาวมากให้เป็น "ท่อนๆ" (Chunks) 
    *   *การตั้งค่า:* แต่ละท่อนยาว 800 ตัวอักษร และมีส่วนเหลื่อมกัน (Overlap) 150 ตัวอักษร เพื่อไม่ให้เนื้อความขาดตอน
3.  **Embedding:** ส่งข้อความแต่ละท่อนไปที่ API (OpenRouter) เพื่อแปลงเป็น **Vector (พิกัดตัวเลข)**
    *   *โมเดลที่ใช้:* `openai/text-embedding-3-small` (แปลงข้อความเป็นพิกัด 1,536 มิติ)
4.  **Storage:** บันทึกเนื้อความ (Content) คู่กับพิกัด (Embedding Vector) ลงในฐานข้อมูล SQLite

### เฟสที่ 2: การค้นหาและตอบคำถาม (Retrieval & Generation Phase)
เกิดขึ้นทุกครั้งที่คุณพิมพ์คำถามในแชท
1.  **Query Embedding:** ระบบนำ "คำถามของคุณ" ไปแปลงเป็นพิกัดตัวเลข (Vector) โดยใช้โมเดลเดียวกัน
2.  **Vector Search:** นำพิกัดของคำถามไปเปรียบเทียบกับพิกัดของ Chunks ทั้งหมดในฐานข้อมูล
    *   ใช้การคำนวณทางคณิตศาสตร์ที่เรียกว่า **Cosine Similarity** เพื่อหาว่า Chunks ไหน "มีความหมายใกล้เคียง" กับคำถามที่สุด
3.  **Retrieval:** ดึงข้อมูล 5 อันดับแรก (Top-K) ที่เกี่ยวข้องที่สุดออกมา
4.  **Augmentation:** นำข้อมูลทั้ง 5 ส่วนนั้นไปวางรวมกับ System Prompt เพื่อบอก AI ว่า "นี่คือข้อมูลอ้างอิง โปรดใช้ข้อมูลนี้ตอบคำถาม"
5.  **Generation:** AI ส่งคำตอบกลับมาโดยระบุแหล่งที่มา (Source File) ให้คุณทราบด้วย

---

## 3. ทำไมเราถึงเลือกใช้แนวทางนี้?
*   **API-Based Embedding:** เราใช้ `text-embedding-3-small` ของ OpenAI ผ่าน OpenRouter เพราะเป็นโมเดลที่เก่งที่สุดและราคาถูกมาก
*   **Serverless Friendly:** การย้ายจากการใช้ Library หนักๆ (อย่าง Torch/Sentence-Transformers) มาเป็น API ทำให้แอปมีขนาดเล็กลงมาก จนสามารถรันบน **Vercel** ได้
*   **Pure Python Similarity:** เราเขียนฟังก์ชันคำนวณคณิตศาสตร์เองโดยไม่ใช้ `numpy` เพื่อลดขนาด Dependency ให้เหลือน้อยที่สุด

---

## 4. แหล่งที่มาของ Vector Database (Internal Only)
ในโปรเจกต์นี้เราไม่ได้ใช้บริการภายนอก (เช่น Pinecone) แต่เราอาศัยการประยุกต์ใช้ฐานข้อมูลมาตรฐานดังนี้:
*   **Localhost:** ใช้ SQLite (ไฟล์ `database.db`)
*   **Vercel:** ใช้ Vercel Postgres (SQL ฐานข้อมูลมาตรฐาน)
*   **การจัดเก็บ:** ข้อมูล Vector จะถูกเก็บไว้ในตาราง `knowledge_chunks` โดยเก็บพิกัดตัวเลขในรูปแบบ **JSON String** ลงในคอลัมน์ `embedding` ซึ่งวิธีนี้ช่วยให้ระบบทำงานได้เร็ว มีจุดเสียสลายน้อย และไม่มีค่าใช้จ่ายเพิ่มเติมครับ

---

**สรุปสถานะล่าสุด:** 
- ไฟล์ความรู้ถูก Index เรียบร้อยแล้ว (32 Chunks)
- ระบบพร้อมใช้งานทั้งบน Localhost และ Vercel ครับ
