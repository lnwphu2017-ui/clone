# 🧠 ขั้นตอนการทำ Embedding (การเปลี่ยนข้อความเป็นตัวเลข)

ในระบบ RAG ของเรา **Embedding** คือกระบวนการแปลงข้อความภาษาไทย/อังกฤษ ให้กลายเป็นชุดตัวเลข (Vector) เพื่อให้คอมพิวเตอร์สามารถ "คำนวณความเหมือน" ของความหมายได้

---

## 🛠️ 1. โมเดลที่เราเลือกใช้
เราใช้โมเดลชื่อ **`paraphrase-multilingual-MiniLM-L12-v2`** จากค่าย Sentence-Transformers
- **ภาษา:** รองรับได้มากกว่า 50 ภาษา รวมไปถึงภาษาไทยและอังกฤษ
- **รูปแบบ:** เป็น Local Model (ทำงานที่เครื่องเรา 100% ไม่เสียค่า API)

---

## 🔍 3. กลไกการค้นหา (Vector Search Mechanism)

เมื่อเรามีตัวเลข (Vector) แล้ว ระบบจะใช้หลักการทางคณิตศาสตร์ที่เรียกว่า **"Cosine Similarity"** ในการค้นหาครับ

### 📐 หลักการ "องศาความเหมือน"
ให้นึกภาพว่าทุกประโยคคือ **"ลูกศร"** ที่พุ่งออกจากจุดศูนย์กลาง:
- ถ้าประโยคมี **ความหมายเหมือนกันมาก** ลูกศรจะพุ่งไปใน **ทิศทางเดียวกัน** (ทำมุม 0 องศาต่อกัน) ค่าคะแนนจะเป็น **1.0**
- ถ้าประโยค **ไม่เกี่ยวข้องกันเลย** ลูกศรจะพุ่งไป **คนละทิศทาง** (ทำมุม 90 องศาต่อกัน) ค่าคะแนนจะเป็น **0.0**

### 💻 บรรทัดโค้ดที่รันจริง:
ไฟล์: [api/rag.py](file:///c:/DayOne1/api/rag.py) | บรรทัดที่ 166
```python
def ComputeCosineSimilarity(v1, v2):
    # สูตรคณิตศาสตร์: หาค่า Cosine ของมุมระหว่างลูกศร 2 อัน
    # ยิ่งค่าเข้าใกล้ 1.0 แปลว่าคุยเรื่องเดียวกัน!
    dot_product = np.dot(v1, v2)
    norm_v1 = np.linalg.norm(v1)
    norm_v2 = np.linalg.norm(v2)
    return dot_product / (norm_v1 * norm_v2)
```

---

## 🔄 4. ขั้นตอนการทำงานจริง (Step-by-Step)

### ขั้นตอนที่ 1: โหลดโมเดล (Model Loading)
ไฟล์: [api/rag.py](file:///c:/DayOne1/api/rag.py) | บรรทัดที่ 35
```python
model = SentenceTransformer('paraphrase-multilingual-MiniLM-L12-v2')
```
ระบบจะโหลดโมเดลเตรียมไว้ในหน่วยความจำ (RAM) เพื่อพร้อมใช้งานทันที

### ขั้นตอนที่ 2: แปลงข้อความเป็นตัวเลข (Vectorization)
เมื่อมีข้อความเข้ามา (ทั้งตอนเก็บข้อมูลและตอนผู้ใช้ถาม) ระบบจะเรียกฟังก์ชัน `EmbedTexts`
```python
def EmbedTexts(texts: List[str]) -> List[List[float]]:
    # โมเดลจะเปลี่ยนประโยค เช่น "วิชา Database" ให้เป็นตัวเลข 384 มิติ
    # [0.12, -0.05, 0.88, ...]
    return model.encode(texts).tolist()
```

### ขั้นตอนที่ 3: บันทึกลงฐานข้อมูล
เมื่อได้ตัวเลขมาแล้ว เราจะเก็บลงใน SQLite ในรูปแบบ JSON String เพื่อรอการค้นหา
- **ตาราง:** `knowledge_chunks`
- **ฟิลด์:** `embedding` (เก็บลิสต์ตัวเลขยาวๆ)

### ขั้นตอนที่ 4: การคำนวณความเหมือน (Cosine Similarity)
เมื่อผู้ใช้ถาม เช่น "ปี 2 มีอะไรบ้าง?"
1. ระบบจะแปลงคำถามนั้นเป็น **ตัวเลข (Question Vector)**
2. นำตัวเลขไป "เทียบ" กับตัวเลขของทุก Chunk ในฐานข้อมูล
3. คำนวณหาค่าคะแนน (0.0 ถึง 1.0) ว่าตัวไหน "ความหมายใกล้กันที่สุด"

---

## 🌟 ทำไมถึงต้องทำ Embedding?
หากเราใช้การค้นหาด้วยคำทั่วไป (Keyword Search) แล้วคุณพิมพ์ว่า **"วิชาฐานข้อมูล"** แต่ในเอกสารเขียนว่า **"Database Systems"** ระบบธรรมดาจะหาไม่เจอ
**แต่ระบบ Embedding จะ "เข้าใจ" ว่าสองคำนี้มีความหมายเดียวกัน** และดึงข้อมูลมาให้คุณได้อย่างถูกต้องครับ!

---
*จัดทำโดย: Antigravity AI | 2026-04-08*
